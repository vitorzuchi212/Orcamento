@model Orcamento.Models.OrcamentoGeral

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>OrcamentoGeral</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

           
            <div class="form-group">
                <label asp-for="Gastos" class="control-label">Descrição</label>
                <input asp-for="Gastos" class="form-control" />
                <span asp-validation-for="Gastos" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Receitas" class="control-label">Descrição</label>
                <input asp-for="Receitas" class="form-control" />
                <span asp-validation-for="Receitas" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
